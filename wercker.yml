box: wercker/default
build:
  steps:
    - arjen/hugo-build@1.1.0:
        theme: hugo-zen
        flags: --buildDrafts=true
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        basedir: public
