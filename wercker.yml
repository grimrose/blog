box: wercker/default
build:
  steps:
    - arjen/hugo-build@1.1.0
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        basedir: public
        domain: grimrose.github.io
  after-steps:
    - sherzberg/slack-notify:
        subdomain: grimrose
        token: $SLACK_API_TOKEN
        channel: "#general"
        username: wercker
        icon_url: https://avatars3.githubusercontent.com/u/1695193?s=140
